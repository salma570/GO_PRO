public class PriorityQueue{
  public static void main(String [] args){
    PriorityQueue<Integer> pq = new PriorityQueue<>(); //queue FIFO

    // is a queue in Ascending order by default
    // elements could be repeated

// For reversed order
    PriorityQueue<Integer> pq1 = new PriorityQueue<>(Collections.reverseOrder()); //queue FIFO

    // have all operations as the queue
    // most operations' complexity  O(log(n))

    












    
  }
}

public class Stack{
  public static void main (String[] args){
    
    
    Stack<Integer> st = new Stack<>(); 
    // can add to size in bracket (2) eg.
    //write all types complete 
    //the fist character kman majuscule
//add mubers to the stack 
    st.push(6);
    st.push(5);
    st.push(4);
    st.push(0);
    st.push(9);
    
    st.pop(); //remove the top element 
    st.peak(); // get the eleement on top
    st.size(); //gets u the stack size

    //remove all eleements
    while(!stack.isEmpty()) {
            System.out.println(stack.pop());
        }
    
  }


  
}
